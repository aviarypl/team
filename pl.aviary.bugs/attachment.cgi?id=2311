diff -r a2b028e252fe browser/chrome/browser/devtools/appcacheutils.properties
--- a/browser/chrome/browser/devtools/appcacheutils.properties	Sun May 26 23:37:30 2013 +0200
+++ b/browser/chrome/browser/devtools/appcacheutils.properties	Mon May 27 10:47:20 2013 +0200
@@ -78,5 +78,5 @@ cacheManifestOnlyFirstLine="CACHE MANIFE
 # to validate a cache manifest using AppCacheUtils.jsm having an asterisk (*) in
 # a section other than the network section.
-asteriskInWrongSection=Gwiazdka użyta jako wildcard w sekcji %S w linii %S. Pojedyncza linia zawierająca gwiazdkę jest nazywana flagą wildcard białej listy online i jest prawidłowa tylko w sekcji NETWORK. Inne użycie znaku * jest niedozwolone. Obecność tej flagi wskazuje, że dowolny adres URI nie znajdujący się na liście przechowywanych w pamięci podręcznej może być niejawnie traktowany jako będący w przestrzeniach nazw białej listy online. Jeśli tej flagi nie ma, stan blokowania wskazuje, że adresy URI nie znajdujące się na explicite na liście w manifeście mają być traktowane jako niedostępne.
+asteriskInWrongSection=Gwiazdka użyta jako wildcard w sekcji %S w linii %S. Pojedyncza linia zawierająca gwiazdkę jest nazywana flagą wildcard białej listy online i jest prawidłowa tylko w sekcji NETWORK. Inne użycie znaku * jest niedozwolone. Obecność tej flagi wskazuje, że dowolny adres URI nieznajdujący się na liście przechowywanych w pamięci podręcznej może być niejawnie traktowany jako będący w przestrzeniach nazw białej listy online. Jeśli tej flagi nie ma, stan blokowania wskazuje, że adresy URI nieznajdujące się na explicite na liście w manifeście mają być traktowane jako niedostępne.
 
 # LOCALIZATION NOTE (escapeSpaces): Used when an attempt is made to validate a
diff -r a2b028e252fe browser/chrome/browser/devtools/debugger.properties
--- a/browser/chrome/browser/devtools/debugger.properties	Sun May 26 23:37:30 2013 +0200
+++ b/browser/chrome/browser/devtools/debugger.properties	Mon May 27 10:47:20 2013 +0200
@@ -213,3 +213,3 @@ functionSearchSeparatorLabel=←
 # The substitution parameter is the URL of the last paused window that must be
 # resumed first.
-resumptionOrderPanelTitle=Jeden lub wiele debugerów jest wstrzymany. Należy najpierw przywrócić ostanio wstrzymany debuger przy: %S
+resumptionOrderPanelTitle=Jeden lub wiele debugerów jest wstrzymanych. Należy najpierw przywrócić ostatnio wstrzymany debuger przy: %S

